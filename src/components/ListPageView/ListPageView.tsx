import { observer } from 'mobx-react-lite';

type TListPageViewProps = {
  content: any
  isEditable: boolean
}

export const ListPageView = observer(({
  content,
  isEditable
}: TListPageViewProps) => {
  return (
    <></>
  )
})